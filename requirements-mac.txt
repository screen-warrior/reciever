# macOS RECEIVER dependencies only.
# The Mac only runs the receiver, so it does not need the sender GUI (PyQt6),
# the Windows `keyboard` lib, or PyInstaller.

# HTTP listener
fastapi==0.115.0
uvicorn==0.30.6

# Global hotkeys (F9 / Esc) on macOS
pynput==1.7.7

# Keystroke injection via Quartz CGEvent
pyobjc-framework-Quartz>=10.1
